user: vp
packages:
    - mc
    - git
    - vim
    - zsh
    - curl
    - sudo
    - wget
    - tmux
    - htop
    - iftop

py_modules:
    - pip
    - virtualenv
    - virtualenvwrapper

rc_files:
    - {
        src: 'rc-.tmux.conf',
        dst: '.tmux.conf'
      }
    - {
        src: 'rc-.vimrc',
        dst: '.vimrc'
      }
    - {
        src: 'rc-.zshrc',
        dst: '.zshrc'
      }
    - {
        src: 'rc-.profile',
        dst: '.profile'
      }
    - {
        src: 'rc-.zprofile',
        dst: '.zprofile'
      }
